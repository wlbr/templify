language: go
sudo: false
go:
  - tip
before_install:
  - export PATH=".:${PATH}"
  - go get github.com/mattn/goveralls
  - make all
  - make examples
script:
  - $HOME/gopath/bin/goveralls -service=travis-ci